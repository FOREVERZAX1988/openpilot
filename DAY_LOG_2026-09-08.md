
## 2026-09-08 VcruiseSync 释放间隙修复 + 单位澄清

### 用户问题答复
- stock_wunschgeschw = ACC_Wunschgeschw_02 原生 = km/h（DBC Unit_KiloMeterPerHour）
- cruiseState.speed = stock × KPH_TO_MS = m/s（标准字段契约，必须 m/s）
- Fix①②里 v_cruise_kph = cruiseState.speed × MS_TO_KPH ≡ stock_wunschgeschw（幂等往返，无误差）
- 两个 fix 仅判 carFingerprint==PORSCHE_MACAN_MK1，无条件生效，与 MacanVcruiseSync 开关无关

### 本次提交（主仓 8e3aaf0b7 / opendbc 7fd40d3e0）
- 修复 st=6 直接根因：VcruiseSync 每批按键后 0 释放帧 → LS_01 置位几乎不间断
  → Cabana 像按键黏连/卡键 → 原厂误判 → st6 关功能
- 新增 _SYNC_RELEASE_FRAMES=10(100ms)：每批按键最后一帧后强制释放间隙
- 每批按键 = 干净单脉冲（20-80ms 按住 + 100ms 松手）

### 分支状态
- 两仓库均在 macan-long-0908，已推送远程（FOREVERZAX1988/openpilot + /opendbc）
- 快照 snap_1788831062 "macan-0908-vcruise-gap"

### 待用户确认的事项
- 用户仍有几个问题待澄清（OP vCruise 退出纵向后是否做初始化巡航速度的操作）

## 2026-09-08 追加：Fix①②取值统一 + VcruiseSync 定参(80ms+20ms)

### 用户问题确认
- **stock_wunschgeschw 单位**：原厂 = km/h（DBC Unit_KiloMeterPerHour），确认无误，之前的"M/s"标注是笔误/误判，实际 carstate 存的是 km/h。
- **Fix② 应否也从 stock_wunschgeschw 取值**：是。当前用 CS.cruiseState.speed × MS_TO_KPH 是幂等往返 ≡ stock_wunschgeschw（因为 cruiseState.speed = stock × KPH_TO_MS）。因为 card.py 的 CS 是 capnp CarState（无 stock_wunschgeschw 属性），所以用的就是这条幂等等价路径，值一致。
- **为什么 cruiseState.speed = ACC_Wunschgeschw_02 × KPH_TO_MS 又乘回来**：cruiseState.speed 是 openpilot 标准字段契约必须 m/s（核心代码都假设 m/s），而 DBC 原生是 km/h。所以先转 m/s 进标准字段，需要 km/h 时再乘 MS_TO_KPH 转回。这是桥接两个单位体系的标准做法，非冗余。
- **两个 fix 是否无条件生效**：是。仅判 carFingerprint==PORSCHE_MACAN_MK1，与 MacanVcruiseSync 开关无关。

### 修复 bug：Fix① 死代码
- card.py 上下文 CS=capnp CarState，无 stock_wunschgeschw 属性 → 原 getattr(CS,'stock_wunschgeschw',0.0) 恒 0.0 → Fix① 从未生效。
- 已改为 CS.cruiseState.speed × MS_TO_KPH（幂等往返），与 Fix② 一致。

### VcruiseSync 定参（用户指定）
- 问题不是窗口太小而是太频繁 → 窗口固定 80ms、松手 20ms 后才允许发第二个信号。
- 每批 = 80ms 按住 + 20ms 释放 = 干净单脉冲，杜绝黏连误判 st6。
- 失败升档逻辑 → 固定 80ms 连续 3 次未生效 → 放弃+冷却5s。

### 提交
- opendbc 5ce022cb1，主仓 7ba4d51f7（含 opendbc bump），均已推送 macan-long-0908

## 用户定稿需求（待确认后实施，未改动代码）
1. 327.36=无巡航速度排除：巡航速度≤120km/h；原厂速度>120 时不执行 SET 置入（OP 代码本就限 120，原厂 120+ 不追平）。最大保持 140km/h。
2. 速度差判定需连续 N 帧（5~10 帧）持续有速度差才模拟 LS 按键去追平——当前 stop_and_go.py 是单帧 delta 判定，需加连续帧滤波。
3. 80ms 窗口已固定 + 释放间隔待确认：当前 _SYNC_RELEASE_FRAMES=2(20ms)；用户后定稿为 80ms 窗口 + 间隔 100ms(_SYNC_RELEASE_FRAMES=10)。
